import React from "react";
import type {IOrder}  from "../../types";
interface IOrderMapProps {
  order: IOrder;
}
const OrderMap = ({order}: IOrderMapProps) => {
  return <div>OrderMap</div>;
};

export default OrderMap;
